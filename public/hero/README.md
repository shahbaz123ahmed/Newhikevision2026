# Hero Images Directory

Save your custom category or subcategory hero background images inside this folder.
For example, save `fingerprint-terminals.jpg` here, and it will be accessible at `/hero/fingerprint-terminals.jpg`.

Then, ensure it is mapped inside the `HERO_IMAGES` dictionary inside `app/products/[...slug]/page.tsx`.
